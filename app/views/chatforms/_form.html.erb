<%= form_for(@chatform) do |f| %>
  <% if @chatform.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chatform.errors.count, "error") %> prohibited this chatform from being saved:</h2>

      <ul>
      <% @chatform.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
